import type { User } from '@/types';

// NOTE: Mock data is being phased out. Use fetched data where available.

// MOCK USER - Replace with actual auth user context
export const MOCK_USER: User = {
  // ... existing mock user data ...
};

// MOCK CAMP CONTEXT - Replace with actual camp context/data
export const MOCK_CAMP = {
  // ... existing mock camp data ...
};

// MOCK USER PROFILE - Remove this (handled by ProfilePage fetching)
// export const MOCK_USER_PROFILE = { ... };

// MOCK ANNOUNCEMENTS - Will be replaced by DB fetch
export const MOCK_ANNOUNCEMENTS = [
  // ... existing mock announcements ...
];

// MOCK LISTS & ITEMS - Will be replaced by DB fetch
export const MOCK_LISTS = [
  // ... existing mock lists ...
];

export const MOCK_LIST_ITEMS = [
  // ... existing mock list items ...
];
